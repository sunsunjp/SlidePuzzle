import React from 'react'
import ReactDOM from 'react-dom/client'
import SlidePuzzle from './slide-puzzle.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SlidePuzzle />
  </React.StrictMode>,
)
